/// <reference types="react" />
import { Profile } from "index";
declare type Props = {
    iconUrl: Profile.IconUrl;
};
declare const IconImage: (props: Props) => JSX.Element;
export default IconImage;
