/// <reference types="react" />
import { Tag as TagData } from "index";
declare type Props = {
    tags: Array<TagData>;
};
declare const TagArea: (props: Props) => JSX.Element;
export default TagArea;
